# Data Cleaning 
1. *rem duplicates* df.drop_duplicates(), .reset_index(), dropna().
2. *fixing structural errors* -> type parsing, misspellings, inconsistent vals, cols rename.
3. *Filter unwanted outliers* -> conditional filtering in pandas. z score filtering.
4. *Missing data handelling* -> removal, sum() , average(),  forward-backward fill, interpolation.  